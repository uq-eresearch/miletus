%div.page-header
  %h1 Metadata Records


%div.row-fluid
  %div.span4
    %h2 Stats

    %ul.unstyled
      - @stats.each do |k,v|
        %li
          %span.lead{ :style => 'display: inline-block; width: 3em; text-align: right' }
            = v
          %span= k

  %div.span8
    %h2 Concepts

    %table.table
      %thead
        %tr
          %th.span4 Title
          %th Facets
          %th Size
          %th Last Modified
      %tbody
        - @concepts.each do |c|
          %tr
            %td
              %a{:href => concept_path(c.key)}
                = titles(Nokogiri::XML(c.to_rif)).first
            %td= c.facets.count
            %td= "#{("%s" % c.to_rif).length} bytes"
            %td= c.updated_at