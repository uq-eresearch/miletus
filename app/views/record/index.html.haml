.page-header
  %h1 Metadata Records

.row-fluid

  #concepts.span12

    %ul.nav.nav-pills
      - @types.sort.each do |ct|
        %li.active.type-toggle
          %a{:href => '#',
             'data-type' => ct,
             :title => 'Click to toggle '+ct.pluralize}
            = ct.pluralize.titleize

    - @concepts.each do |c|
      %p{'data-type' => c.type}
        = type_image(c.type)
        %a{:href => concept_path(c.id)}
          = c.title
