%h1 OAI-PMH RIF-CS Record Collections

- @collections.each do |rc|
  %h2= rc.endpoint

  %table.table
    %thead
      %tr
        %th Identifier
        %th Datestamp
        %th Size
    %tbody
      - rc.records.each do |r|
        %tr
          %td= r.identifier
          %td= r.datestamp
          %td= "#{r.metadata.to_s.length} bytes"